The basic set of instructions:

-add a vagrant box.

-get appserver tar.gz package

-extract it.

-install git in your VM

-clone git repo

-add user "app"

yum install perl-GD.x86_64
#this adds prereqs that you need 

add localhost as virtual host to apache conf.  set its dir as /opt/appserver/apache/htdocs



